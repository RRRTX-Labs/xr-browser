# Python (tools/ — governance tooling)
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# Build artifacts (P2+ — do not commit from P1)
out/
build-xr/
*.log

# P2/P3 working dirs (never committed)
# chromium/ = default pinned checkout (build/sync.py convention)
chromium/
# work/ = upstream-tracking workspace (P3 rebase bot, work/upstream-cache/)
work/
