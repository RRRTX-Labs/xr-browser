# evidence/P12/human-gates.md — the human gates P12 leaves open

**Provenance.** Authored by the P12 agent on 2026-09-17. Every row names an act
this sandbox cannot perform and must not fake. Where a row is carried from P11
the wording says so; where P12 adds a gate, the reason is stated. No row below
asserts a result that a command in this repository produced, and no row
substitutes a sandbox number for a browser measurement.

| id | gate | why human | operator steps (exact) | status |
|---|---|---|---|---|
| HG-31 | Browser-rig page-level assertions for the cosmetic/scriptlet surface | needs a real browser, renderer and DOM — none exist here. `gn` and `ninja` are ABSENT (measured, not assumed), so no Chromium build is possible and any "no blank pages" claim would be fabricated | `docs/qa/browser-harness.md`: build at the pin, run the document-start timing harness, the blank-page detection sweep on the fixture corpus, and the 50-hard-apps spot check with `xr_shield_cosmetic_v1` in BOTH states | NOT-RUN (the guard is verified by static lint + core-level tests; the page-level assertion runs here, with this method) |
| HG-20 | PAT revocation | user action, user-owned credential | revoke the GitHub PAT supplied in the P12 dispatch chat, and rotate anything it ever touched. The token was used only for the push the orchestrator explicitly instructed, was never written into any file in either tree, and `.git/config` is not persisted in the workspace snapshot | OPEN (carried from P9; still user-owned). **P12 note:** a token was supplied in chat again — see `evidence/P11/human-gates.md`, which now cross-references this row |
| HG-26 | Ratify-or-amend the living-contract consumers before the next phase | sign-off act | `docs/contracts/FROZEN.yaml` carries 14 rows, all `ratified: PENDING`, now six consumers deep. P12 adds a seventh candidate surface (`cosmetic-blob-v1`) as a LIVING contract — deliberately not frozen, because its consumers have not been built. Review `docs/contracts/registry-post-freeze.md` and ratify in one batch or record the amendment | OPEN (recommended: one-batch ratification of the existing 14; the cosmetic contract stays living until P12's host and validator ship) |
| HG-34 | Branch protection on `main` in both repos | org setting, not a repo artifact | GitHub → Settings → Branches → require signed DCO commits + green `governance` before merge on `RRRTX-Labs/xr-browser` and `RRRTX-Labs/xr-core` | OPEN |
| — | `sast` scheduled lane: verify the next fire is green | scheduled cadence, not a push-time act | the last `sast` run at the P11 tip is `STALE-FAIL`: the workflow file was fixed at `35d0993` after that run started, so its verdict predates the fix. `tools/scheduled_lane_check.py` reports this exactly — visible, non-fatal, never counted green. Confirm the next fire, and the new `snapshot-freshness` job added by P12-T0-a | OPEN (standing check; P12 adds one more scheduled lane to watch) |
| HG-28 | 24 h full-mutation matrices at the release pin | wall-clock farm budget | `tools/mutation_test.py --targets <all> --sample 0` per core at the release pin, including the cosmetic core once it lands | PARTIAL (P11's local full matrices are in `docs/state/mutation-scores.json`: 7 cores, shield 396/396, update 215/216 with one dispositioned equivalent mutant. The cosmetic core's matrix is not run here) |
| — | Perf budget rows are browser measurements | needs a real browser + the reference uBO build | the four cosmetic rows P12 adds (decide p99, apply p99, memory, DOM-poll) are generated by `tools/gen_perf_budgets.py`, never hand-edited, and carry `rig_class: trend` until measured on the rig. Method: `docs/shield/perf.md` + `docs/qa/browser-harness.md` | NOT-RUN (a trend rig may never assert MET by rule; sandbox numbers are NEUTRAL) |
| — | Corpus parity against a 1,000-site browser capture | needs a browser to capture with | the in-repo corpus is band-checked against the vendored uBO-derived fixture; the real capture set is farm. Method: `docs/shield/parity.md` | NOT-RUN (method committed, no number invented) |
| HG-9 / HG-27 | Real mojom bindings replacing `fakes/*.py` | farm glue; requires a Chromium build | build `//xr/renderer/cosmetic` in-tree at the pin and drive the cosmetic interface from the renderer process instead of the stdio fake | NOT-RUN (the fakes are labelled behavioural fakes, never bindings — `fakes/README.md`) |
| — | Patch review by a Chromium-side reviewer | the `0300-cosmetic-document-start` patch touches Blink | the patch must stay ≤25 files under `blink_seams`, carry `patchinfo.md` with the upstream pin and the `#if defined(ENABLE_XR_SHIELD)` guard, and pass `build/webui/shield_seam_roundtrip.py` (apply/verify/revert byte-exact). A human must read the seam diff before it is proposed upstream | NOT-RUN for the cosmetic patch (the 0200 precedent is proven byte-exact every run) |
| — | Upstream-first ledger entries for cosmetic | filing to upstream Chromium is a human act | every divergence from upstream gets a ledger entry with a real issue URL. Inventing a chromium issue URL is a hard stop; where no upstream issue exists the entry says so explicitly | OPEN (P11's precedent: `docs/shield/parity-divergences.md` D-5…D-9, no invented URLs) |
