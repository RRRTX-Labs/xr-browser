P11-T5 EVIDENCE TRANSCRIPT — BlockEvent → ring + Activity Ledger (living block-event-v1)
=========================================================================================
Date: 2026-09-13 (Asia/Karachi). Executor: XR P11 Agent (Senior Staff Browser Engineer role).
Brief: /home/user/uploads/PHASE-P11.md §T5 (L463–467) + §architecture invariant 7 (L356–364)
+ registration laws (L88–100) + plan data-laws (L195/228/255).

COMMITS
-------
xr-core (chain of 3, DCO-signed):
  44f5306d7805ea473e5423a4496fd7be162befa4  P11-T5 (xr-core side): the living
      block-event-v1 row + event-emit emitter (7 files: shield/core/events.{h,cc},
      shield/host/shield_host.cc, fakes/shield.py, shield/host_protocol.md,
      shield/tests/test_events.cc, shield/tests/test_shield_host.cc)
  1e5fa01  debt fix: the 18 vendored Cargo.lock files never committed (upstream
      crates' own .gitignore files swallowed them at T1's git add; hosted
      shield-vendor lane red since; verified via git-archive extraction)
  341f1fb  debt fix: fakes/shield.py call() flag default "on" (xrctl generic
      path TypeError on the hosted governance lane since T2)
xr-browser: DEPS pin 341f1fbe33cd… + T5 commit <TBD after commit> — new:
  block-event-v1.{md,schema.json}, golden-block-event.json, test_block_event.py,
  reason-codes.{md,json}, shield_vector_families_t5.py, negatives/p11_t5.sh;
  modified: gen_shield_vectors.py, xr_schema.py, xrctl.py (tuple unwrap),
  p11_gates.sh, run_negatives.sh, fetch_allowlist_check.py (EXAMPLE_URL port
  fix), tools/tests/test_p11_t0d_evidence.py (hermeticity pins),
  corpus-shield.json (40), shield-v1.json (274), registry-post-freeze.md,
  research-log-P11.md (D7 ×12 items), mutation-scores.json (shield 394/394),
  DEPS

HOSTED-CI DEBT (found + discharged in T5; research-log D7 items 9–12)
----------------------------------------------------------------------
Hosted governance + core-hardening were RED at the T3/T4 pushes (runs at
06bd7ab7 + 9df51d1) — pushed without querying CI (T1 checked its runs; T2–T4
did not). T5 queried the API, root-caused ALL reds, fixed each with a named
commit, and adopts a new evidence law: every push is followed by a hosted-run
query recorded in the phase transcript.
  1. shield-vendor lane: 18 × MISSING Cargo.lock → xr-core 1e5fa01.
  2. governance pytest: xrctl TypeError on shield.Status → xr-core 341f1fb
     (flag default) + xrctl tuple unwrap (browser side).
  3. governance pytest: test_p11_t0d_evidence rule-(e) fixtures leaked the
     environment (hosted has cargo/go; sandbox doesn't) → hermeticity pins
     via monkeypatch; proven under a simulated hosted PATH (fake
     cargo/go/rustc/clang/semgrep binaries: 12/12 green).

DELIVERABLES (brief T5 asks → where each landed)
------------------------------------------------
1. block-event-v1 living contract + schema, registered post-freeze:
   docs/contracts/block-event-v1.md + block-event-v1.schema.json (house strict
   subset); registry-post-freeze.md P11 table row (LIVING — superset around the
   FROZEN BlockEvent mojom vocabulary; never widens it; policy-change-event-v1
   untouched); review packet docs/contracts/review/04-shield-v1.md (existing).
   FROZEN.yaml / INDEX.md NOT touched.
2. Emission through the existing ring/Activity-Ledger path (invariant 7):
   new host method event-emit (shield_host.cc + fakes/shield.py byte-identical);
   MakeLedgerRow in shield/core/events.cc is the ONLY row constructor; redaction
   AT CREATION (RedactTarget — query/fragment/port never reach target); caller-
   supplied seq/ts_millis (no clock; dispatch.cc precedent); chip counter is the
   only passive surface the emitter may drive (law stated in host_protocol.md +
   contract + reason-codes.md; enforcement extraction stays T6 per plan).
3. Events carry rule id/list/version/site-class, never query strings:
   row fields rule_id, rule (filter text), list_id, bundle_version,
   origin{scheme,registrable_domain}, request_class + redacted target.
4. Retention/caps inherited from P8 ring tests:
   ring FIFO cap kCapacity=256 + kChunkBudgetBytes=65536 (existing P8-inherited
   tests, untouched); doc/code-match law pytest-asserted (the attention-budget
   pattern); 90-day rolling default retention + per-identity local-only storage
   + user export + never-uploaded stated as P13 persist-time dependency in the
   contract (documented honestly, not claimed as implemented).
5. Schema validation on BOTH backends:
   tools/xr_schema.py SCHEMAS["block-event"]; golden
   docs/contracts/tests/golden-block-event.json generated by the COMPILED C++
   host and byte-verified against the fake (t5_golden_gen.py asserts
   h_out == f_out; test_block_event.py re-asserts from the committed bytes);
   golden rides the vector law too (e-emit-golden-instance case).
6. claims_lint-clean "why blocked" reason-code table (P13 renders text):
   docs/shield/reason-codes.{md,json} — the 11 closed why_codes → stable
   shield.why.* text keys + default actions; five-way sync (host_protocol
   closed set = schema enum = table json = table md = vectors' usage) asserted
   in docs/contracts/tests/test_block_event.py; claims_lint --file gate wired
   into p11_shield_gates.

LATENT BUG FOUND + FIXED (recorded as research-log D7 item 3)
-------------------------------------------------------------
mojom/shield.mojom declares BlockEvent.ts_millis/tab_id int64, but the T2-era
EventToJson cast both to int32 — epoch-scale timestamps truncated on the wire
(no committed value was large enough to show it; all T2 fixtures used small
ints). Fixed in EventToJson AND MakeLedgerRow (static_cast<int64_t>); frozen-
surface bytes unchanged for every committed value (test_golden_vectors 355
checks green before and after); the law is now pinned by golden ts_millis
1750000000123 + vectors e-emit-epoch-ts / e-emit-big-ints.

GATE RESULTS
------------
xr-core C++ suites (make -C shield/tests test): ALL PASS —
  test_context 63, test_bundle_strict 79, test_posture 59, test_scope 44,
  test_apply 43, test_events 53 (+15 T5), test_match 76, test_shield_host 51
  (+13 T5), test_golden_vectors 423 (+68: the 274-case set from the compiled
  side), test_sha256_kat 10. 0 failures.
Emitter parity probe (t5_parity_probe.py): 43/43 byte-identical host vs fake
  (happy full/defaults, all 4 action k-spellings, all 11 why_codes, redaction,
  epoch-scale int64 seq/ts/tab_id/bundle_version, 24 malformed-token cases).
Golden generation (t5_golden_gen.py): 403-byte canonical row, both backends
  byte-identical, redaction asserted (no utm_source/newsletter in bytes).
Vectors: 229 → 274 (+45 fam_events: golden instance, every why_code, every
  action, redaction incl. port-stripping, zero boundaries, http origin, int64
  law, 22 malformed tokens). gen --check PASS (byte-identical regen);
  shield_vectors_check PASS (274 byte-identical across shield_host + fake).
Parity corpus: 35 → 40 cases / 12 → 13 methods (5 event-emit cases: happy,
  defaults, bad-action, bad-why, missing-seq); parity_completeness PASS
  (6 pairs, 0 failures, every documented method covered).
pytest docs/contracts/tests/test_block_event.py: 19/19 PASS (golden bytes from
  both backends, schema strictness 9 mutations refused, five-way vocab sync,
  vectors-pin-golden, ring cap + retention doc/code match).
Mutation matrix (shield lane, FULL, sampled:false, seed 20260913, pin 44f5306):
  394 mutants (389 + 5 new), 394 killed, 0 survivors, 100.00% (gate ≥90),
  deny-guard 61/61, elapsed 205.0s → PASS, FIRST-RUN clean (transcript
  evidence/P11/logs/t5-mutation-shield.json; record + comment updated in
  docs/state/mutation-scores.json).
mutation_freshness: PASS — pin 44f5306d7805, 7/7 cores fresh.
Negatives (tools/run_negatives.sh): N=95 (90 + 5 T5 canaries), ALL REJECTED AS
  EXPECTED, rc=0, 2 pre-existing visible SKIPs (tool absent — never a PASS).
  T5 canaries: mutated golden action reddens xr_schema (+control), dropped
  reason code reddens the five-way pytest (+control), fake emitter that leaks
  raw urls reddens gen --check DRIFT (+control), event-emit corpus gap reddens
  parity_completeness, deleted event-emit protocol row reddens
  host_protocol_check (+control).
freeze_check PASS · registry_lint PASS (new living row) · vocab_lint PASS ·
release-notes train-152 --check PASS (2026-09-13).
fetch_allowlist_check: PASS after the EXAMPLE_URL reserved-domain regex was
  widened to allow a port (`.example:8443/` is exactly as unfetchable as
  `.example/`; research-log D7 item 8 — regex fix, not a file exemption).
full run_checks (tools/run_checks.sh, log committed at
  evidence/P11/logs/t5-runchecks-full.txt — the repo .gitignore
  excludes *.log, so the run log is committed as .txt, the
  t4-mutation-shield-run1-fail.txt precedent): RUN_CHECKS_RC=0 —
  "== ALL GOVERNANCE CHECKS PASSED ==", 125 PASS lines, 0 FAIL, tail
  section (P9 meta) executed (no fatal-gate abort). Highlights:
  - pytest tools/tests: 333 passed (hermeticity-fixed t0d canaries incl.)
  - touched-file size law: PASS (range scan; the T5 files enter the range
    at the browser commit — all ≤380: test_block_event.py 208,
    shield_vector_families_t5.py 116, p11_t5.sh 156, xrctl.py 114,
    test_p11_t0d_evidence.py 161, fetch_allowlist_check.py 175)
  - amend_guard PASS (stamped=True; the warn-only unlisted-registry draft
    note on 9df51d1 is the pre-existing T4 pattern)
  - host_protocol_check: 5 hosts, every method literal documented
    (event-emit row ↔ code literal cross-checked)
  - vectors: 274 regenerate byte-identical + replay byte-identical across
    shield_host (C++) and fakes/shield.py
  - differential_fuzz: 2755 requests, 0 divergences (up from 2594 at T4 —
    the new method is fuzzed through the same seeded generator)
  - mojom_fuzz_gen 4000 cases · fuzz_fleet 4 targets × 60 s clean ·
    server fuzz 585,621 iters / 0 violations
  - mutation-freshness: pin 341f1fbe33cd, 7/7 cores fresh
  - xr-lists round-trip: 15 cells executed (real gpg) · release-notes
    train-152 --check PASS · evidence strict: 8 bundles 0 failures ·
    runner-caps ledger PASS · citation-audit 23/23 · checker-mutation
    meta 3/3 (perf_gate, surfaces_check, evidence_check)
  - visible SKIPs only where tools are absent (actionlint; never a pass)
negatives (tools/run_negatives.sh): N=95, ALL REJECTED AS EXPECTED, rc=0
  (2 pre-existing visible SKIPs); run against the same bytes as committed.
pytest docs/contracts/tests (the CI-only lane, run locally for evidence):
  26 passed (test_block_event.py 19 + parity/determinism/conformance 7),
  including the hermeticity-proven run under a simulated hosted PATH
  (fake cargo/go/rustc/clang/semgrep on PATH: 12/12 t0d canaries green).

OPERATIONAL NOTES
-----------------
- Workspace-snapshot pruning gotcha (NEW, session-level): directories named
  build/ (also dist/, out/, node_modules/, target/, …) are excluded from the
  between-turn workspace snapshot — tracked files under them (xr-browser
  build/**, scripts/**; xr-core common/tests/build/**,
  third_party/rust/vendor/thiserror-1.0.69/build/probe.rs) VANISHED at the
  turn boundary and the P10 negative canaries reddened on missing files.
  Recovered with targeted `git restore <paths>` + vendor_check re-PASS (59
  crates). LAW: never blanket `git restore .` mid-task — the first recovery
  attempt wiped all tracked T5 edits (gen wiring, xr_schema, gates,
  NEG_FILES, corpus, registry, research-log, the regenerated vectors);
  re-applied via t5_browser_patch.py (idempotent anchors) + vector regen.
  At turn start: `git status` both repos and restore excluded-name deletions.
- xr_schema.py --repo is a GLOBAL flag (precedes the subcommand) — the first
  p11_t5.sh canary invocation put it after `validate` and died on
  unrecognized-arguments; fixed, re-run green.
- events.h intent cleanup: the T2 intent header cited speculative
  event-sink-v1/event-stream-v1 names never registered anywhere — replaced
  with the registered block-event-v1 (D7 item 6).

PUSH + HOSTED CI (the new D7-item-9 law)
-----------------------------------------
xr-core pushed FIRST (the pin-alive gate requires the DEPS pin fetchable
from origin): cfbb344..341f1fb → origin/main = 341f1fb (3 commits:
44f5306 T5, 1e5fa01 vendor-lock fix, 341f1fb fake-flag fix).
xr-browser commit + push: immediately after this transcript is committed
(the commit carries it; a sha cannot self-reference). Hosted run
conclusions for the pushed head are queried via the actions API and
recorded in the final report section ⑩ + the session log — the phase is
not called complete on local green alone anymore.

STATUS vs BRIEF T5
------------------
COMPLETE: living contract + schema registered post-freeze; emission through
the ring/ledger path as events (never policy); privacy posture (rule
id/list/version/site-class; query strings structurally absent); retention/caps
inherited + doc/code-matched; xr_schema validation both backends via a
C++-generated byte-verified golden; claims_lint-clean reason-code table with
stable P13 text keys (documented P13 dependency). Nothing widened frozen;
FROZEN.yaml/INDEX.md untouched; attention enforcement extraction left to T6
per plan. Deviations: none. HG flags: none new (P13 render + retention
enforcement recorded as dependencies in the contract/reason docs).
