# Icon set sources — interim marks (P2-T6)

- `xr-mark.svg` — master mark, **own-work, CC0-1.0**, generated by
  `gen_icon.py` (run it to regenerate; byte-identical output).
- **Status:** interim marks **pending HG-5 trademark filings**. Replacement
  after trademark outcomes is **HUMAN-GATED (HG-14)**.
- Rasterization (`.png` at 16/32/48/…/256, `.ico`, `.icns`) is a packaging-time
  step (P13/P39) via `rsvg-convert`/`inkscape`; no rasterized binary is
  vendored in the meta repo. Determinism: the SVG embeds no timestamp.
- These are **not** the shipping brand assets — they exist so the branding
  patch can reference a real (non-placeholder) mark today.
