# Effective Policy — the truth table (generated)

> GENERATED by `tools/vectors_to_md.py` from
> `docs/contracts/vectors/policy-resolver-v1.json` — do not edit by hand.
> Regenerate: `python3 tools/vectors_to_md.py --write`
> CI enforces `--check` (regeneration must be diff-clean).

Contract: `effective-policy-v1` (frozen P5; `FROZEN.yaml`). Resolver:
`xr-core/policy` (C++ core, byte-parity with `xr-core/fakes/policy_resolver.py`
on every vector below). The resolver is TOTAL: unknown/absent input ⇒ the
fully-denying policy; there is no "unset ⇒ allow" state (L3: never guess).

## The dial (Standard / Shield / Fortress)

Exactly three positions (§2.6 — unreadable at five). The tier orders
everything monotonically: higher tier = strictly more restrictive, never
less. Identity attributes compose with the tier: an *ephemeral* identity
always gets in-memory storage; a *fortress* identity always gets the
Fortress partition and a dedicated process, whatever the dial says.

| Field | Standard | Shield | Fortress |
|---|---|---|---|
| block ads/trackers/HTTPS-upgrade | on | on | on |
| cosmetic filtering + scriptlets | off | on | on |
| permissions (geo/cam/mic) | ask | deny | deny |
| notifications | ask | ask | deny |
| third-party egress | allowed | blocked | blocked |
| route | direct | proxy | tor |
| fingerprint | reduce | reduce | strict |
| storage (persistent identity) | identity-scoped | identity-scoped | fortress partition |
| autofill (navigation/permission contexts) | on | on | on |
| vault export | off (always) | off (always) | off (always) |
| site isolation / dedicated process | on / off | on / off | on / on |
| letterbox | off | off | on |

## Deny policy (unknown identity / bad origin / unknown class / malformed)

The deny-default is a VALUE, not an error: all blocking on, all permissions
`kDeny`, egress `kDirect` with third-party blocked, fingerprint `kStrict`,
storage `kEphemeral` + in-memory, no autofill, no export, letterbox on.
The single error arm is `kVersionMismatch` (contract_version ≠ 1),
surfaced deny-safe. Every vector below marked `deny` proves a path to it.

## Exceptions (never "forever" in-flow — §1.5, §11.9)

In-flow exception prompts offer **once / session / 7d only**. `permanent`
lives solely in Settings: a stored `permanent` exception is honored only
with `granted_by: settings` (v2 schema); the store REJECTS
permanent + in_flow. Expiry is evaluated at the resolver's INPUT against a
caller-supplied `now_ms` (purity): `7d` entries are active while
`expires_at > now_ms` (fail-closed at equality), `session` while the
session id matches, `once` while `remaining_uses > 0` (the caller
decrements — the resolver never mutates state).

## Enterprise (T7: a layer, not a second brain)

Managed policy may clamp the tier UP (`trust_floor`) and force fields
(`force_fingerprint`, `force_route`, `force_letterbox`,
`force_block_third_party`) AFTER tier resolution. It is signed-or-ignored:
an unsigned, badly-signed, or unverifiable managed doc is NEVER enforced
(ledger row records it). Enterprise cannot force `export_allowed` true —
the schema's const-false outranks every layer.

## Precedence ladder

1. Enterprise floor/forces (signed layer — highest)
2. Explicit request trust (the ⌥ dial for this context)
3. Active exception (in-flow, expiring)
4. Per-identity site binding (⌥-stored)
5. Per-site default binding
6. `kStandard`

Invalid layer entries are dropped — never widened — with a ledger row per
drop; a malformed core request resolves to the deny policy.

## Golden vectors (66)

| vector | request | expected |
|---|---|---|
| `known-standard-trust_none-kNavigation` | `xr:00000000-000…` @ https://example.com, —, kNavigation | Standard-like (storage kIdentityScoped, route kDirect) |
| `known-standard-trust_none-kScript` | `xr:00000000-000…` @ https://example.com, —, kScript | Standard-like (storage kIdentityScoped, route kDirect) |
| `known-standard-trust_none-kPermission` | `xr:00000000-000…` @ https://example.com, —, kPermission | Standard-like (storage kIdentityScoped, route kDirect) |
| `known-standard-trust_kStandard-kNavigation` | `xr:00000000-000…` @ https://example.com, kStandard, kNavigation | Standard-like (storage kIdentityScoped, route kDirect) |
| `known-standard-trust_kStandard-kScript` | `xr:00000000-000…` @ https://example.com, kStandard, kScript | Standard-like (storage kIdentityScoped, route kDirect) |
| `known-standard-trust_kStandard-kPermission` | `xr:00000000-000…` @ https://example.com, kStandard, kPermission | Standard-like (storage kIdentityScoped, route kDirect) |
| `known-standard-trust_kShield-kNavigation` | `xr:00000000-000…` @ https://example.com, kShield, kNavigation | Shield-like (storage kIdentityScoped, route kProxy) |
| `known-standard-trust_kShield-kScript` | `xr:00000000-000…` @ https://example.com, kShield, kScript | Shield-like (storage kIdentityScoped, route kProxy) |
| `known-standard-trust_kShield-kPermission` | `xr:00000000-000…` @ https://example.com, kShield, kPermission | Shield-like (storage kIdentityScoped, route kProxy) |
| `known-standard-trust_kFortress-kNavigation` | `xr:00000000-000…` @ https://example.com, kFortress, kNavigation | Fortress-like (storage kFortressPartition, route kTor) |
| `known-standard-trust_kFortress-kScript` | `xr:00000000-000…` @ https://example.com, kFortress, kScript | Fortress-like (storage kFortressPartition, route kTor) |
| `known-standard-trust_kFortress-kPermission` | `xr:00000000-000…` @ https://example.com, kFortress, kPermission | Fortress-like (storage kFortressPartition, route kTor) |
| `known-fortress-trust_none-kNavigation` | `xr:00000000-000…` @ https://example.com, —, kNavigation | Standard-like (storage kFortressPartition, route kDirect) |
| `known-fortress-trust_none-kScript` | `xr:00000000-000…` @ https://example.com, —, kScript | Standard-like (storage kFortressPartition, route kDirect) |
| `known-fortress-trust_none-kPermission` | `xr:00000000-000…` @ https://example.com, —, kPermission | Standard-like (storage kFortressPartition, route kDirect) |
| `known-fortress-trust_kStandard-kNavigation` | `xr:00000000-000…` @ https://example.com, kStandard, kNavigation | Standard-like (storage kFortressPartition, route kDirect) |
| `known-fortress-trust_kStandard-kScript` | `xr:00000000-000…` @ https://example.com, kStandard, kScript | Standard-like (storage kFortressPartition, route kDirect) |
| `known-fortress-trust_kStandard-kPermission` | `xr:00000000-000…` @ https://example.com, kStandard, kPermission | Standard-like (storage kFortressPartition, route kDirect) |
| `known-fortress-trust_kShield-kNavigation` | `xr:00000000-000…` @ https://example.com, kShield, kNavigation | Shield-like (storage kFortressPartition, route kProxy) |
| `known-fortress-trust_kShield-kScript` | `xr:00000000-000…` @ https://example.com, kShield, kScript | Shield-like (storage kFortressPartition, route kProxy) |
| `known-fortress-trust_kShield-kPermission` | `xr:00000000-000…` @ https://example.com, kShield, kPermission | Shield-like (storage kFortressPartition, route kProxy) |
| `known-fortress-trust_kFortress-kNavigation` | `xr:00000000-000…` @ https://example.com, kFortress, kNavigation | Fortress-like (storage kFortressPartition, route kTor) |
| `known-fortress-trust_kFortress-kScript` | `xr:00000000-000…` @ https://example.com, kFortress, kScript | Fortress-like (storage kFortressPartition, route kTor) |
| `known-fortress-trust_kFortress-kPermission` | `xr:00000000-000…` @ https://example.com, kFortress, kPermission | Fortress-like (storage kFortressPartition, route kTor) |
| `known-ephemeral-trust_none-kNavigation` | `xr:00000000-000…` @ https://example.com, —, kNavigation | Standard-like (storage kEphemeral, route kDirect) |
| `known-ephemeral-trust_none-kScript` | `xr:00000000-000…` @ https://example.com, —, kScript | Standard-like (storage kEphemeral, route kDirect) |
| `known-ephemeral-trust_none-kPermission` | `xr:00000000-000…` @ https://example.com, —, kPermission | Standard-like (storage kEphemeral, route kDirect) |
| `known-ephemeral-trust_kStandard-kNavigation` | `xr:00000000-000…` @ https://example.com, kStandard, kNavigation | Standard-like (storage kEphemeral, route kDirect) |
| `known-ephemeral-trust_kStandard-kScript` | `xr:00000000-000…` @ https://example.com, kStandard, kScript | Standard-like (storage kEphemeral, route kDirect) |
| `known-ephemeral-trust_kStandard-kPermission` | `xr:00000000-000…` @ https://example.com, kStandard, kPermission | Standard-like (storage kEphemeral, route kDirect) |
| `known-ephemeral-trust_kShield-kNavigation` | `xr:00000000-000…` @ https://example.com, kShield, kNavigation | Shield-like (storage kEphemeral, route kProxy) |
| `known-ephemeral-trust_kShield-kScript` | `xr:00000000-000…` @ https://example.com, kShield, kScript | Shield-like (storage kEphemeral, route kProxy) |
| `known-ephemeral-trust_kShield-kPermission` | `xr:00000000-000…` @ https://example.com, kShield, kPermission | Shield-like (storage kEphemeral, route kProxy) |
| `known-ephemeral-trust_kFortress-kNavigation` | `xr:00000000-000…` @ https://example.com, kFortress, kNavigation | **deny** (storage kEphemeral, route kTor) |
| `known-ephemeral-trust_kFortress-kScript` | `xr:00000000-000…` @ https://example.com, kFortress, kScript | **deny** (storage kEphemeral, route kTor) |
| `known-ephemeral-trust_kFortress-kPermission` | `xr:00000000-000…` @ https://example.com, kFortress, kPermission | **deny** (storage kEphemeral, route kTor) |
| `rclass-sweep-kNavigation` | `xr:00000000-000…` @ https://example.org, kStandard, kNavigation | Standard-like (storage kIdentityScoped, route kDirect) |
| `rclass-sweep-kSubresource` | `xr:00000000-000…` @ https://example.org, kStandard, kSubresource | Standard-like (storage kIdentityScoped, route kDirect) |
| `rclass-sweep-kScript` | `xr:00000000-000…` @ https://example.org, kStandard, kScript | Standard-like (storage kIdentityScoped, route kDirect) |
| `rclass-sweep-kPermission` | `xr:00000000-000…` @ https://example.org, kStandard, kPermission | Standard-like (storage kIdentityScoped, route kDirect) |
| `rclass-sweep-kStorage` | `xr:00000000-000…` @ https://example.org, kStandard, kStorage | Standard-like (storage kIdentityScoped, route kDirect) |
| `rclass-sweep-kNetwork` | `xr:00000000-000…` @ https://example.org, kStandard, kNetwork | Standard-like (storage kIdentityScoped, route kDirect) |
| `unknown-origin-scheme` | `xr:00000000-000…` @ ftp://example.com, —, kNavigation | **deny** (storage kEphemeral, route kDirect) |
| `empty-domain` | `xr:00000000-000…` @ https://, —, kNavigation | **deny** (storage kEphemeral, route kDirect) |
| `unknown-identity` | `xr:deadbeef-000…` @ https://example.com, —, kNavigation | **deny** (storage kEphemeral, route kDirect) |
| `unknown-request-class` | `xr:00000000-000…` @ https://example.com, —, kBogus | **deny** (storage kEphemeral, route kDirect) |
| `malformed-identity` | `'nope'` @ https://example.com, —, kNavigation | **deny** (storage kEphemeral, route kDirect) |
| `malformed-origin-missing` | `xr:00000000-000…` @ None, —, kNavigation | **deny** (storage kEphemeral, route kDirect) |
| `malformed-empty` | `None` @ None, —, ∅ | **deny** (storage kEphemeral, route kDirect) |
| `invalid-trust` | `xr:00000000-000…` @ https://example.com, kSuper, kNavigation | **deny** (storage kEphemeral, route kDirect) |
| `version-mismatch` | `xr:00000000-000…` @ https://example.com, —, kNavigation, cv=999 | **error:** `kVersionMismatch` |
| `determinism-pair-a` | `xr:00000000-000…` @ https://bank.example, kFortress, kNavigation | Fortress-like (storage kFortressPartition, route kTor) |
| `determinism-pair-b` | `xr:00000000-000…` @ https://bank.example, kFortress, kNavigation | Fortress-like (storage kFortressPartition, route kTor) |
| `http-scheme-standard` | `xr:00000000-000…` @ http://legacy.example, —, kNavigation | Standard-like (storage kIdentityScoped, route kDirect) |
| `pad-a.example-kStorage` | `xr:00000000-000…` @ https://a.example, kShield, kStorage | Shield-like (storage kIdentityScoped, route kProxy) |
| `pad-a.example-kNetwork` | `xr:00000000-000…` @ https://a.example, kShield, kNetwork | Shield-like (storage kIdentityScoped, route kProxy) |
| `pad-a.example-kSubresource` | `xr:00000000-000…` @ https://a.example, kShield, kSubresource | Shield-like (storage kIdentityScoped, route kProxy) |
| `pad-b.example-kStorage` | `xr:00000000-000…` @ https://b.example, kShield, kStorage | Shield-like (storage kIdentityScoped, route kProxy) |
| `pad-b.example-kNetwork` | `xr:00000000-000…` @ https://b.example, kShield, kNetwork | Shield-like (storage kIdentityScoped, route kProxy) |
| `pad-b.example-kSubresource` | `xr:00000000-000…` @ https://b.example, kShield, kSubresource | Shield-like (storage kIdentityScoped, route kProxy) |
| `pad-c.example-kStorage` | `xr:00000000-000…` @ https://c.example, kShield, kStorage | Shield-like (storage kIdentityScoped, route kProxy) |
| `pad-c.example-kNetwork` | `xr:00000000-000…` @ https://c.example, kShield, kNetwork | Shield-like (storage kIdentityScoped, route kProxy) |
| `pad-c.example-kSubresource` | `xr:00000000-000…` @ https://c.example, kShield, kSubresource | Shield-like (storage kIdentityScoped, route kProxy) |
| `pad-d.example-kStorage` | `xr:00000000-000…` @ https://d.example, kShield, kStorage | Shield-like (storage kIdentityScoped, route kProxy) |
| `pad-d.example-kNetwork` | `xr:00000000-000…` @ https://d.example, kShield, kNetwork | Shield-like (storage kIdentityScoped, route kProxy) |
| `pad-d.example-kSubresource` | `xr:00000000-000…` @ https://d.example, kShield, kSubresource | Shield-like (storage kIdentityScoped, route kProxy) |

*End of generated document.*
